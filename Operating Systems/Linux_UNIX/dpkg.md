# dpkg

Debian Package Manager

## Debian package files

`.deb` extension

### Install

```cmd
# or --install
sudo dpkg -i  package_file.deb
```

### Uninstall

```cmd
# or --remove
sudo dpkg -r package_file.deb
```
